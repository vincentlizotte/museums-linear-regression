# NOTE: Normally, the config file wouldn't be checked into source control, but this time it was done to make the demo easier to install and run.
# Usually, this template would be used by the developers and the build to make the proper config file for that environment


# Copy this and rename it to config.py
#database_connection_string = "sqlite:///:memory:"
database_connection_string = "sqlite:///database/database.db"
database_echo = True
